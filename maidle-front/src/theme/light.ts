import { ThemeConfig } from "antd"
import "@fontsource/figtree"

const config: ThemeConfig  = {
  token: {
    // Seed Token
    fontFamily: "Figtree",
    
    // Alias Token
  },
}

export default config
